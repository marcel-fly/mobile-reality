{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --port 3005 --middlewares ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "json-server db.json --port 3005 --middlewares ./server.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
